#include<stdio.h>
int main()
{
  int a,b,dig1=0,dig2=0;
  scanf("%d%d",&a,&b);
  int arr[a][b];

  for(int i=0;i<a;i++)
  {
      for(int j=0;j<b;j++)
      {
      scanf("%d",&arr[i][j]);
      }

  }
 int x,flag=0;
 scanf("%d",&x);
 for(int i=0;i<a;i++)
  {
      for(int j=0;j<b;j++)
      {
       if(x==arr[i][j])
       {
           flag = 1;
           break;
       }
      }

  }
  if(flag) printf("will not take number");
  else printf("will take number");
  return 0;
}
