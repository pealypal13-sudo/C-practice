#include<stdio.h>
#include<math.h>
int main()
{
  int n;
  scanf("%d",&n);
  int prime=1;
  for(int i=2;i<n;i++)
  {
    if(n%i==0)
    {
      prime=0;
      break;
    }
  
  }
  if(prime==1)
  {
    printf("YES");
  }
  else{
    printf("NO");
  }
  
  return 0;
}
